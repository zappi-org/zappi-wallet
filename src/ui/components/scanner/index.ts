export { UnifiedScanner } from './UnifiedScanner'
export type { UnifiedScannerProps } from './UnifiedScanner'

// Types are now in @/core/domain/input-types
// Detection/validation is now via useInputParser() hook
